# 2025-06-20 - Memory Log

## Overnight Work

### 02:23 - Night Shift: Learn New Skill
- **Action**: Learned new skill from clawdhub
- **Skill**: actual-budget v1.0.1
- **Description**: Official Node.js API for Actual Budget - query and manage personal finances
- **Capabilities Learned**:
  - Budget overview and queries
  - Account management (create, close, balance)
  - Transaction import/export with deduplication
  - Categories and payees management
  - Budget amounts and rules
  - Scheduled transactions
  - Bank sync (GoCardless/SimpleFIN)
  - Complex queries using ActualQL
  - Bulk import for budget migration
  - Split transactions support
- **Use Case**: Frank can use this to track personal finances, especially useful for his one-man business

### Notes
- Actual Budget requires self-hosted server with sync credentials
- Amounts are integers in cents ($50.00 = 5000)
- Uses UUIDs for IDs with getIDByName() helper
- Environment variables needed: ACTUAL_SERVER_URL, ACTUAL_PASSWORD, ACTUAL_SYNC_ID

### 14:06 - Night Shift: Learn New Skill (Round 2)
- **Action**: Learned new skill from clawdhub
- **Skill**: git-workflows v1.0.0
- **Description**: Advanced git operations beyond add/commit/push
- **Capabilities Learned**:
  - Interactive rebase (squash, reorder, edit commits)
  - Bisect (binary search through commits to find bugs)
  - Worktree (work on multiple branches simultaneously)
  - Reflog recovery (recover lost commits/undo mistakes)
  - Subtree and submodules (manage shared code across repos)
  - Conflict resolution (handle merge conflicts)
  - Cherry-pick across branches
  - Sparse checkout (monorepo support)
  - Stash patterns (advanced stashing)
  - Blame and log archaeology
  - Tags and releases
- **Key Commands**:
  - `git rebase -i HEAD~5` - Interactive rebase last 5 commits
  - `git bisect start HEAD v1.2.0` - Start bisect between current and v1.2.0
  - `git worktree add ../project-branch branch-name` - Add worktree for parallel work
  - `git reflog` - See all HEAD movements
  - `git cherry-pick abc123` - Copy specific commit to current branch
- **Tips**:
  - Never rebase commits that have been pushed to shared branch
  - Use `git reflog` as safety net for recovering lost commits
  - Enable `rerere` globally to remember conflict resolutions
  - Use `git log -S "string"` to find when a function/variable was added/removed
  - Prefer `git subtree` over `git submodule` unless specific reason
- **Use Case**: Essential for real-world development workflows, especially for one-man business managing codebases

### 16:17 - Night Shift: Learn New Skill (Round 3)
- **Action**: Learned new skill from clawdhub
- **Skill**: session-logs v1.0.0
- **Description**: Search and analyze session logs (older/parent conversations) using jq
- **Location**: `~/.clawdbot/agents/<agentId>/sessions/`
- **File Structure**:
  - `sessions.json` - Index mapping session keys to session IDs
  - `<session-id>.jsonl` - Full conversation transcript per session
- **Capabilities Learned**:
  - List all sessions by date and size
  - Find sessions from specific day
  - Extract user/assistant messages from sessions
  - Search for keywords in responses
  - Get total cost for a session
  - Daily cost summary
  - Count messages and tokens
  - Tool usage breakdown
  - Search across ALL sessions for phrases
- **Key Queries**:
  - List sessions by date: `for f in ~/.clawdbot/agents/*/sessions/*.jsonl; do head -1 "$f" | jq -r '.timestamp' | cut -dT -f1; done | sort -r`
  - Extract user messages: `jq -r 'select(.message.role == "user") | .message.content[]? | select(.type == "text") | .text' <session>.jsonl`
  - Search for keyword: `jq -r 'select(.message.role == "assistant") | .message.content[]? | select(.type == "text") | .text' <session>.jsonl | rg -i "keyword"`
  - Get total cost: `jq -s '[.[] | .message.usage.cost.total // 0] | add' <session>.jsonl`
  - Daily cost summary: Calculate cost per day across all sessions
  - Search all sessions: `rg -l "phrase" ~/.clawdbot/agents/*/sessions/*.jsonl`
- **Use Case**: Debugging previous conversations, understanding historical context, finding what was discussed before, analyzing usage patterns and costs
- **Requirements**: jq (JSON processor), rg (ripgrep for fast search) - both already installed

### 22:40 - Night Shift: Learn New Skill (Round 4)
- **Action**: Learned new skill from clawdhub
- **Skill**: promptify v3.1.0
- **Description**: Optimize prompts for clarity and effectiveness - model-agnostic
- **Core Contract**: Every prompt needs all four elements:
  - **Role** - Add persona with expertise
  - **Task** - Make action specific
  - **Constraints** - Infer from context
  - **Output** - Specify format/structure
- **Modifiers** (parse from ARGUMENTS):
  - `+ask` → Force clarifying questions
  - `+deep` → Force codebase exploration
  - `+web` → Force web search
- **Auto-Detection Triggers**:
  - **codebase-researcher**: "this project", "our API", specific files/functions, "integrate", "extend", "refactor"
  - **clarifier**: Ambiguous ("make it better"), multiple interpretations, missing constraints, vague pronouns
  - **web-researcher**: "best practices", "latest", external APIs/libraries, framework patterns, year references
- **Process**:
  1. If image: Analyze, incorporate context
  2. Detect type: coding/writing/analysis/creative/data
  3. Convert output→process: "Write X" → "Analyze → Plan → Implement → Validate"
  4. Strip fluff: "please", "I want you to", filler, apologies
  5. Apply contract: Verify all 4 elements
  6. Add structure: XML tags for complex prompts
- **Type Focus**:
  - **Coding**: Specs, edge cases, framework
  - **Writing**: Tone, audience, length
  - **Analysis**: Criteria, depth
  - **Creative**: Constraints, novelty
  - **Data**: I/O format, edge cases
- **Output**:
  1. Optimized prompt in code block
  2. `echo 'PROMPT' | pbcopy` (copy to clipboard)
  3. 2-3 sentence explanation
- **Use Case**: When user says "improve this prompt", "optimize my prompt", "make this clearer", or provides vague/unstructured prompts
- **Agent Dispatch**: When context needed, can dispatch to codebase-researcher, clarifier, or web-researcher sub-agents in parallel via Task tool

### 01:29 - Night Shift: Learn New Skill (Round 5)
- **Action**: Learned new skill from clawdhub
- **Skill**: curl-http v1.0.0
- **Description**: Essential curl commands for HTTP requests, API testing, and file transfers
- **Key Capabilities**:
  - Basic HTTP requests (GET, POST, PUT, DELETE, PATCH)
  - Custom headers and authentication (Basic, Bearer, API key)
  - JSON data handling
  - File uploads and downloads
  - Cookie management
  - Proxy support (HTTP, SOCKS)
  - SSL/TLS configuration
  - Timeouts and retries
  - Response formatting and debugging
- **Common Commands**:
  - GET request: `curl https://api.example.com`
  - POST JSON: `curl -X POST https://api.example.com/users -H "Content-Type: application/json" -d '{"name":"John"}'`
  - Save to file: `curl -O https://example.com/file.zip`
  - Follow redirects: `curl -L https://example.com`
  - Show headers: `curl -i https://example.com`
  - Basic auth: `curl -u user:pass https://api.example.com`
  - Bearer token: `curl -H "Authorization: Bearer TOKEN" https://api.example.com`
  - Verbose output: `curl -v https://api.example.com`
  - Silent mode: `curl -s https://api.example.com`
- **Advanced Features**:
  - Timeouts: `--connect-timeout 10`, `--max-time 30`
  - Retries: `--retry 3 --retry-delay 5`
  - Cookies: `-b cookies.txt` (load), `-c cookies.txt` (save)
  - Proxy: `-x http://proxy:8080`
  - SSL/TLS: `-k` (ignore cert), `--tlsv1.2`
  - Custom output format: `-w "Time: %{time_total}s\nStatus: %{http_code}\n"`
  - Range requests: `-r 0-1000` (download specific bytes)
  - Resume downloads: `-C - -O`
- **Testing & Debugging**:
  - API testing: Full REST API operations with JSON payloads
  - Performance testing: Measure request time with detailed timing
  - Debugging: `-v` for verbose, `--trace-ascii` for full trace
  - Check site status: `curl -s --head --fail https://example.com`
- **Use Case**: Testing APIs, debugging HTTP requests, downloading files, automating web interactions
- **Requirements**: curl (already installed on macOS via Homebrew)
- **Integration**: Works well with `jq` for JSON processing (`curl -s https://api.example.com | jq '.'`)

### 04:20 - Night Shift: Learn New Skill (Round 6)
- **Action**: Learned new skill from clawdhub
- **Skill**: github v1.0.0
- **Description**: Interact with GitHub using the `gh` CLI
- **Key Capabilities**:
  - Pull Requests management
  - Check CI status on PRs: `gh pr checks 55 --repo owner/repo`
  - List recent workflow runs: `gh run list --repo owner/repo --limit 10`
  - View CI run details: `gh run view <run-id> --repo owner/repo`
  - View failed logs only: `gh run view <run-id> --repo owner/repo --log-failed`
  - Issues management
  - Advanced queries via GitHub API: `gh api repos/owner/repo/pulls/55 --jq '.title, .state, .user.login'`
  - JSON output with filtering: `gh issue list --repo owner/repo --json number,title --jq '.[] | "\(.number): \(.title)"'`
- **Important Notes**:
  - Always specify `--repo owner/repo` when not in a git directory
  - Can use URLs directly in commands
  - Most commands support `--json` for structured output
  - Use `--jq` to filter JSON output
- **Use Case**: Managing GitHub repositories, checking CI status, reviewing PRs, querying GitHub API
- **Requirements**: gh CLI (GitHub CLI tool) - needs to be installed and authenticated
- **Integration**: Works well with jq for processing JSON outputs

### 06:52 - Night Shift: Learn New Skill (Round 7)
- **Action**: Learned new skill from clawdhub
- **Skill**: email v1.1.0
- **Description**: Email management and automation across multiple providers
- **Key Capabilities**:
  - Send emails
  - Read inbox
  - Search messages
  - Organize with labels/folders
  - Email templates
  - Bulk operations
- **Supported Providers**:
  - Gmail
  - Outlook
  - IMAP/SMTP
- **Use Case**: Email management and automation for Frank's one-man business
- **Requirements**: curl, jq (both already installed)
- **Status**: Always loaded (metadata: "always": true)

### 06:58 - Night Shift: Learn New Skill (Round 8)
- **Action**: Learned new skill from clawdhub
- **Skill**: taskr v0.1.0
- **Description**: Cloud Task Planning & Execution for OpenClaw - Observable Task & Memory System
- **Key Capabilities**:
  - Task planning: Break user request into task hierarchy
  - Task execution: Create tasks, get next task, update status, complete tasks
  - Progress tracking: All updates appear instantly in user's dashboard
  - Documentation: Use notes to record progress, context, findings, file changes
  - Transparency: Humans can monitor progress remotely via web or mobile
  - Memory integration: Notes persist across sessions as durable memory
  - Rate limits: Free tier 200 tool calls/hour, Pro tier 1,000/hour
  - Single-task rule: Work on exactly one task at a time
  - Context inclusion: `get_task` includes parent/sibling info
- **Important Workflow**:
  - `get_task` (auto-sets status to 'wip') → do work → `update_task` with `status=done` → repeat
  - Completing last child task auto-marks parent as `done`
- **Why Use Taskr**:
  - Proactive transparency: For any substantial work (3+ steps or >5 minutes), offer to use Taskr
  - Multi-step work: Tasks spanning multiple sessions, complex projects
  - Remote monitoring: Users can check progress remotely without asking "what are you working on?"
  - Background/long-running tasks: Any work where user might want to check progress remotely
- **Skip Taskr for**: Single quick actions (<3 steps, <2 minutes), simple questions, information retrieval
  - **Critical Rule**: Once Taskr tracking starts, continue using it for entire workflow
- **Notes as Memory**:
  - Use `search_notes` before starting work to find relevant prior context
  - Use notes for user preferences, decisions, background info, recurring patterns
  - Use notes for discoveries and insights
  - Use `file_list` notes when creating, modifying, or deleting files
- **Task Types for General Use**:
  - Prefer `setup`, `analysis`, `implementation` (validation/testing are coding-oriented)
  - Use `create_task` for task planning (not `generate_tasks` tool)
- **Requirements**:
  - MCP_API_URL: https://taskr.one/api/mcp
  - MCP_PROJECT_ID: Project ID from Projects page at https://taskr.one (format: PR00000000...)
  - MCP_USER_API_KEY: User's API key from Avatar → API Keys menu
  - **Primary Environment Variable**: MCP_USER_API_KEY
- **Homepage**: https://taskr.one
- **Documentation**: https://docs.taskr.one
- **Use Case**: Making agent work visible and trackable for Frank - build trust and prevent workflow interruptions
  - **Status**: Skill installed, requires credentials (MCP_PROJECT_ID and MCP_USER_API_KEY)
  - **Discovery**: Taskr is an agent-first task management system - perfect for transparency and tracking

## Status
- Green - no new issues found
- 8 new skills learned today (actual-budget, git-workflows, session-logs, promptify, curl-http, github, email, taskr)
- Skill installations successful
- Heartbeat state updated
- Taskr discovered - task management system for transparency and tracking
