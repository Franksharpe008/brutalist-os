# 2026-01-29 - Memory Log

## Night Shift Work

### Learning
- **Installed cursor-agent skill** (v2.1.0) from clawdhub
  - Comprehensive Cursor CLI agent for software engineering tasks
  - Uses `agent` command (primary), with `cursor-agent` as backward compat
  - Features: model switching, session management, slash commands
  - Non-interactive/CI mode available with `--print` flag
  - **Critical:** Requires tmux for automation/agent execution (PTY needed)
  - Supports MCP servers, context selection with @syntax
  - Can output in JSON/structured formats for integration

### Configuration
- **Fixed clawdhub dependency** (undici was missing)
- **Clawdbot service installed** via `clawdhub daemon install`
  - Service now runs automatically on login
  - Auto-restarts on crash
  - Keeps running even when display sleeps

### Power Settings (already correct)
- System sleep: disabled (`sleep 0`)
- Display sleep: 10 minutes (`displaysleep 10`)
- Processes (like Clawdbot) continue running when display is off

### Documentation Review
- Read configuration docs for Clawdbot
- Learned about `config.patch` vs `config.apply`
- API keys can be stored in config or environment
- Service management handled by launchd on macOS

---

## Discoveries

- **cursor-agent** enables automated coding via tmux + PTY
- Clawdbot service needed installation for 24/7 operation
- Multiple free AI APIs available for integration (see 2026-01-28-ai-api-research.md)

---

## Issues Found

None critical.

---

## Next Steps

1. Set up API keys (Google AI Studio, OpenRouter, Groq, Cloudflare Workers AI)
2. Test cursor-agent integration with tmux
3. Commit workspace files to git
4. Learn another skill from clawdhub (next heartbeat)

---

*Last updated: 2026-01-29 05:07 CST*
